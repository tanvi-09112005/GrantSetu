# Copy to .env.local. Only VITE_-prefixed vars reach the browser bundle —
# never put the service-role key here.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE_URL=/api
